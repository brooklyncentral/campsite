[#ftl]
{
    "db_config" : {
        "client" : "mysql",
        "connection" : {
            "host" : "${databaseHost}",
            "port" : "${databasePort?c}",
            "user" : "${databaseUser}",
            "password" : "${databasePassword}",
            "database" : "campsite",
            "charset" : "UTF8MB4_GENERAL_CI"
        }
    }
}
